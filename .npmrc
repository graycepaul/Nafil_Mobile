# expo-router 57 pulls radix/react-dom peers that conflict with the pinned react
# version in this SDK. Without this, plain `npm install` and `expo install --fix`
# both fail on ERESOLVE.
legacy-peer-deps=true
